# Dev Manual Profile

<!-- factory-version: 1.3.0 -->
> dev-manual tuning. Read by the `dev-manual` agent.
> Last updated: 2026-09-08

## Audience
- Contributors cloning `cagrigider/yoklama` who will change `app.py` or `static/`

## Depth
- TBD (working default: how to run, where code lives, schema rules, HTTP surface, how to add an ADR)

## Sections to include
- Run (`python3 app.py`), loopback bind, SQLite path, stdlib-only rule
- Handler + SPA layout, additive schema, live-tick contract
- PII gitignore, synthetic seed example

## Exclusions
- Operator Grup 8 roster contents
- Architecture memory dump (point to `.factory/ARCHITECTURE.md` instead of duplicating)
