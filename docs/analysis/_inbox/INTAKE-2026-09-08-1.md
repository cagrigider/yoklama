# INTAKE-2026-09-08-1 — Make Yoklama shareable: first-run setup + Settings

- **Source:** chat excerpt (operator Çağrı Gider; productization of existing local app)
- **Received:** 2026-09-08 · **Recorded by:** cagri
- **Form:** chat excerpt
- **Intent:** process improvement (existing-project AS-IS + GAP)
- **Slug:** NEW: `yoklama-setup` · **Confidence:** high (asked)

## Input (verbatim or reference)

Operator asked to make the existing local Yoklama web app generic so other AI Champions can clone it and use it on their own computers. Later confirmed: continue with BA for first-run setup (group name, first meeting + repeat, roster import).

Decisions given in this chat (do not re-litigate):

1. No login, no shared server. Each champion runs the app locally (`127.0.0.1`).
2. Meeting cadence is not assumed biweekly. Setup/Settings: first meeting date, then a repeat rule.
3. Distribution is git (private GitHub `cagrigider/yoklama`), not a zip.
4. Group-specific roster and SQLite must not be in git (`seed/people.json`, `data/attendance.db` gitignored). Repo already created and pushed without that data.
5. Roster: first-run wizard can import Excel/CSV; `people.json` still works. Document required fields in README. Do not require unused fields.
6. Fresh clone: first-run wizard. Afterwards (including the operator’s populated DB): a Settings screen for group name, series, further import. Existing Grup 8 data must not be wiped.
7. Generating a series never deletes. Add missing dates in range; leave existing meetings and marks.
8. Repeat choices: weekday of the first date, then every week / every 2 weeks / every month, plus “don’t repeat”.

Turkish UI, live tick, activity tags, and manager copy stay as in the current app unless a later phase changes them.

## Extracted entities

- **System:** Yoklama (local Python + SQLite + static UI), GitHub `cagrigider/yoklama`
- **Stakeholders:** Çağrı Gider (Grup 8 lead / operator); other AI Champions (same role, own laptop, own group)
- **Out of scope:** login, central server, cross-group rollup, guests, Teams auto-attendance
- **Constraints:** local-only; no PII in git; operator DB already populated
- **Person fields in use today:** `id` (sicil), `name` (required for a row); `position` and `center` shown in UI (optional); `email` stored but not shown — not required
- **Memory:** none under `attendance/docs/analysis/_memory/` — `/factory:ba-init` not run yet

## Impact (change requests only — from the slug's traceability matrix)

Not applicable — no prior Yoklama FRD. AS-IS is the running app in this repo. Treat as existing-product GAP, not a delta against a traceability matrix.

Touched AS-IS surfaces (informal): empty-seed behaviour, first-run vs Settings, roster import, series generation, README field contract, group display name (header currently generic “Yoklama”).

## Open points

- OP-001 · major · answered · Roster source: Excel/CSV import in wizard **and** `people.json`. Why: first-run screen. Disposition: operator, 2026-09-08.
- OP-002 · major · answered · Required import fields = fields the app uses; list them in README. Disposition: required `id`/`sicil` + `name`; optional `position`, `center`/`yetkinlik`; `email` not required. Operator + code, 2026-09-08.
- OP-003 · blocker · answered · Setup when: empty DB → wizard; later Settings for everyone including operator. Do not wipe Grup 8. Disposition: operator, 2026-09-08.
- OP-004 · blocker · answered · Series vs existing meetings: never delete; fill missing dates only. Disposition: operator, 2026-09-08.
- OP-005 · major · answered · Repeat: first date’s weekday; every week / 2 weeks / month / don’t repeat. Disposition: operator, 2026-09-08.

No undispositioned blockers or majors.

## Recommended route

1. `/factory:ba-analyst` — **entry.** Existing-project (AS-IS + GAP) on slug `yoklama-setup`. AS-IS = current local Yoklama; GAP = wizard + Settings + import + series fill-gaps + README field contract.
2. `/factory:ba-modeler` — setup/settings flows and meeting/roster state.
3. `/factory:ba-frd` — FRs/NFRs + traceability.
4. `/factory:ba-uiux` — first-run wizard + Settings screens (Turkish).
5. `/factory:ba-test` — Gherkin for wizard, Settings, import, fill-gaps, no-wipe.
6. `/factory:ba-review`

**Skipped**

- `/factory:ba-services` — no external/partner API; app stays local HTTP + SQLite. Import is file-on-disk, not an integration.
- `/factory:ba-estimate` — not requested.
- `/factory:ba-init` — no Yoklama memory yet; **suggest** it before or with analyst, not a blocker for this record.
- Implementation / `/factory:plan` — after BA review, not this lane.

## Expected artifacts & outcome

Under `docs/analysis/yoklama-setup/`:

- BRD + executive summary (AS-IS + GAP), configured locales
- diagrams (activity/sequence/state as modeler requires)
- FRD/SRS + traceability
- UI/UX screen specs for wizard + Settings
- Gherkin tests covering: empty first-run, Settings on populated DB, Excel and JSON roster, required-field README contract, series fill-gaps never deleting marks
- review audit

Check: another champion can clone empty and set group + series + roster; operator keeps Grup 8 data and can use Settings.

## Memory deltas

none (no `_memory/` in this repo). Suggest `/factory:ba-init`.

## Resolution

Route proposed, not executed. Waiting on operator to run `/factory:ba-analyst`.
